For Java project:
1- generate project with start.spring.io
2- add pom_dependencies.txt to project's pom.xml
3- copy files
4- add model mapper bean in application class:
        @Bean
    	public ModelMapper modelMapper() {
    		ModelMapper modelMapper = new ModelMapper();
    		modelMapper.getConfiguration().setMatchingStrategy(MatchingStrategies.STRICT);
    		return modelMapper;
    	}
5- add component scan:
    @ComponentScan(basePackages = "com.example.demo, ir.mojir.my_kc_auth_client")
6- application.properties:
    server.port=8081
	spring.datasource.url=jdbc:h2:file:./data
	spring.jpa.hibernate.ddl-auto=update
	spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080/realms/cbi
	kc.clientId=demo
    kc.clientSecret=KgwiCz9l6q9JUiNE0o2IoT9GwB2l54VJ


For WUI:
1- ng new demo-wui
2- copy files
3- link my-angular-commons
    IN MY-ANGULAR-COMMONS2 PROJECT:
        ng build my-angular-commons2
        cd dist/my-angular-commons2
        npm link
    IN PROJECT:
        npm link my-angular-commons2
4- ng add @angular/material
5- add routes in app.routes.ts from needed-routes.txt
6- copy styles.css
7- copy assets beside app folder
8- add dir="rtl" in body (index.html)
9- in angular.json -> "projects.project-name.architect.build.options.preserveSymlinks": true
10- ng g environments - then add following config:
    export const environment = {
        keycloak: {
            url: "http://localhost:8080",
            realm: "cbi",
            clientId: "demo-wui"
        }
    };
11- npm install keycloak-js